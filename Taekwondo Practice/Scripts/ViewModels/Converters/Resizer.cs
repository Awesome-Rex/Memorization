using System;
using System.Collections.Generic;
using System.Globalization;
using System.Text;
using System.Windows.Data;

namespace Practice_Dojang.Scripts.ViewModels.Converters
{
    /*class Resizer : IValueConverter
    {
        public object Convert(object value, Type targetType, object parameter, CultureInfo culture)
        {
            try
            {
                App.ResizeType parameterType;
                Enum.TryParse(value.ToString(), out parameterType);
                if (parameterType == App.ResizeType.Add)
                {
                    //return 
                } else if (parameterType == App.ResizeType.Multiply)
                {

                }
            } catch
            {

            }
        }

        public object ConvertBack(object value, Type targetType, object parameter, CultureInfo culture)
        {
            throw new NotImplementedException();
        }
    }*/
}
