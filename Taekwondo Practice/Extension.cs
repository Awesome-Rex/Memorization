using System;
using System.Collections.Generic;
using System.Text;
using System.Windows;
using System.Windows.Controls;

namespace Taekwondo_Practice
{
    namespace Extension
    {
        /*class NavButton : Button
        {
            public string NavText
            {
                get
                {
                    return (string)GetValue(navTextProperty);
                }
                set
                {
                    SetValue(navTextProperty, value);
                }
            }

            // Using a DependencyProperty as the backing store for navText.  This enables animation, styling, binding, etc...
            public static readonly DependencyProperty navTextProperty =
                DependencyProperty.Register("navText", typeof(string), typeof(Button), new PropertyMetadata(0));
        }*/
    }
}
